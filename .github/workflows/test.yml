name: CI

on:
  push:
    paths-ignore:
    - README.md

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: exit 1
      id: exit1
      continue-on-error: true
    - run: |
        echo outcome: ${{ steps.exit1.outcome }}
        echo conclusion: ${{ steps.exit1.conclusion }}
