name: CI

on:
  push:
    paths-ignore:
    - README.md

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: satackey/action-docker-layer-caching@v0.0.4
      with:
        key: some,cache-{hash}

    - run: docker pull satackey/kosen-reports
